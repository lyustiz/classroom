         
        <v-col cols="12" md="6">
            <v-select
            :items="selects.{{fkInstanceName}}"
            item-text="{{fkColTableName}}"
            item-value="{{fkTableId}}"
            v-model="form.{{columnName}}"
            :rules="[rules.select]"
            label="{{labelName}}"
            :loading="loading"
            dense
            ></v-select>
        </v-col>
         