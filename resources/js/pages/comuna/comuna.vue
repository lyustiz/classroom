<template>

    <list-container :title="title" :head-color="$App.theme.headList" @onMenu="onMenu($event)">

        <template slot="HeadTools">
            <add-button @insItem="insertForm()"></add-button>
        </template>

            <v-col cols="12" md="6">
                <v-text-field
                    v-model="search"
                    append-icon="search"
                    label="Buscar"
                    hide-details
                    clearable
                ></v-text-field>
            </v-col>

            <v-data-table
                :headers="headers"
                :items  ="items"
                :search ="search"
                item-key="id"
                :loading="loading"
                sort-by=""
                dense
            >

                <template v-slot:item="{ item }">
                    <tr>
                        <td class="text-xs-left">{{ item.co_comuna }}</td>
						<td class="text-xs-left">{{ item.nb_comuna }}</td>
						<td class="text-xs-left">{{ item.id_zona }}</td>
						<td class="text-xs-left">{{ item.tx_latitud }}</td>
						<td class="text-xs-left">{{ item.tx_longitud }}</td>
						<td class="text-xs-left">{{ item.tx_observaciones }}</td>
						<td class="text-xs-left">
                            <status-switch 
                                :loading="loading" 
                                :item="item" 
                                @onChangeStatus="changeStatus($event)">
                            </status-switch>
                        </td>
                        
                        <td class="text-xs-left">
                            <list-buttons 
                                @update="updateForm(item)" 
                                @delete="deleteForm(item)" >
                            </list-buttons>
                        </td>
                    </tr>
                </template>

            </v-data-table>

            <app-modal
                :modal="modal"
                @closeModal="closeModal()"
                :head-color="$App.theme.headModal"
                :title="title"
            >
                <comuna-form
                    :action="action"
                    :item="item"
                    @closeModal="closeModal()"
                ></comuna-form>

            </app-modal>

            <form-delete
                :dialog="dialog"
                :loading="loading"
                message="Desea eliminar el Registro Seleccionado?"
                @deleteItem="deleteItem()"
                @deleteCancel="deleteCancel()"
            ></form-delete>

            <pre v-if="$App.debug">{{ $data }}</pre>

    </list-container>

</template>

<script>
import listHelper from '@mixins/Applist';
import comunaForm  from './comunaForm';
export default {
    mixins:     [ listHelper],
    components: { 'comuna-form': comunaForm },
    data () {
    return {
        title:    'Comuna',
        resource: 'comuna',
        headers: [
            { text: 'Comuna',   value: 'co_comuna' },
			{ text: 'Comuna',   value: 'nb_comuna' },
			{ text: 'Zona',   value: 'id_zona' },
			{ text: 'Latitud',   value: 'tx_latitud' },
			{ text: 'Longitud',   value: 'tx_longitud' },
			{ text: 'Observaciones',   value: 'tx_observaciones', sortable: false, filterable: false },
			{ text: 'Status',   value: 'id_status' },
            { text: 'Acciones', value: 'actions', sortable: false, filterable: false },
        ],
    }
    },
    methods:
    {
        
    }
}
</script>

<style>
</style>